# 鸿蒙智能家居 AI 助手：设备接入与虚拟仿真测试指南

> 本文档针对 SmartHomeAIHub 项目，为 AI 助手提供真实的设备控制能力，并解决开发测试阶段缺乏实物设备的问题。

---

## 目录

1. [当前项目现状分析](#1-当前项目现状分析)
2. [智能家居设备接入方案](#2-智能家居设备接入方案)
3. [HarmonyOS Connect 生态接入](#3-harmonyos-connect-生态接入)
4. [云平台接入方案](#4-云平台接入方案)
5. [虚拟仿真测试方案](#5-虚拟仿真测试方案)
6. [项目改造实施路径](#6-项目改造实施路径)
7. [推荐开发套件](#7-推荐开发套件)

---

## 1. 当前项目现状分析

### 1.1 当前 AI 助手的工作模式

```
用户: "帮我打开空调调到26度"
         ↓
AI 回复: "已为您打开空调，温度已调节到26度"
         ↓
实际情况: ❌ 设备状态未改变，只是文字模拟
```

**问题根源**：
- `AIService.ets` 只调用 LLM API 获取文字回复
- `DeviceStore.ets` 仅维护本地状态，未与真实设备通信
- 缺少设备控制指令的下发机制

### 1.2 需要补充的核心能力

| 能力模块 | 当前状态 | 需要补充 |
|---------|---------|---------|
| 设备状态存储 | ✅ AppStorage | 保持 |
| AI 对话 | ✅ LLM API | 保持 |
| 意图解析 | ❌ 无 | 新增 |
| 设备控制下发 | ❌ 无 | 新增 |
| 设备协议适配 | ❌ 无 | 新增 |
| 云端通信 | ❌ 无 | 新增 |

---

## 2. 智能家居设备接入方案

### 2.1 主流智能家居通信协议

```
┌─────────────────────────────────────────────────────────────┐
│                      智能家居协议栈                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   应用层                                                      │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │  MQTT   │  │  CoAP   │  │  HTTP   │  │ Matter   │  │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                             │
│   传输层                                                      │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐               │
│   │  TCP    │  │  UDP    │  │ TLS/SSL │               │
│   └──────────┘  └──────────┘  └──────────┘               │
│                                                             │
│   网络层                                                      │
│   ┌──────────┐  ┌──────────┐  ┌──────────┐  ┌──────────┐  │
│   │   WiFi  │  │ Bluetooth│  │ Zigbee  │  │   BLE    │  │
│   └──────────┘  └──────────┘  └──────────┘  └──────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 2.2 协议对比分析

| 协议 | 功耗 | 速率 | 距离 | 适用场景 | 代表产品 |
|------|------|------|------|---------|---------|
| **WiFi** | 高 | 100Mbps+ | 100m | 大功率设备 | 空调、冰箱、洗衣机 |
| **Bluetooth BLE** | 低 | 2Mbps | 10m | 低功耗传感器 | 温湿度传感器、智能门锁 |
| **Zigbee** | 低 | 250Kbps | 100m | 组网设备 | 开关、插座、灯泡 |
| **Matter** | 中 | 高 | 广 | 跨平台互联 | 新一代智能家居 |
| **红外 IR** | 低 | 慢 | 5m | 传统家电控制 | 空调、电视遥控 |

### 2.3 市面主流空调品牌接入方案

#### 方案一：使用品牌官方 SDK

| 品牌 | SDK 名称 | 协议 | 接入难度 | 生态 |
|------|---------|------|---------|------|
| 美的 | Midea Home SDK | MQTT | ⭐⭐ | 美居 App |
| 格力 | Gree Smart+ | HTTP/MQTT | ⭐⭐⭐ | 格力+ App |
| 海尔 |海尔 U+ | MQTT | ⭐⭐ | 智家 App |
| 大金 | 大金 Cloud | HTTP | ⭐⭐⭐⭐ | My Daikin |
| 格力 | 红外码库 | 红外 | ⭐ | 万能遥控 |

#### 方案二：红外遥控方案（通用性强）

```
手机/网关  ──红外发射──→  空调（任意品牌）
                            ↑
                       红外码库匹配
```

**适用场景**：控制任意带遥控器的空调

**红外码库资源**：
- `IRRemoteESP8266` - ESP8266/ESP32 红外控制库
- `ACnirvana` - 通用空调控制库
- 社区共享码库：https://github.com/TonisOjawitt/AC-Ctrl

#### 方案三：智能红外网关

```
手机 App  ──WiFi/MQTT──→  红外网关  ──红外──→  空调
                                      ↑
                                 码库学习/预置
```

**推荐产品**：
- 小米红外网关
- 华为海雀红外遥控器
- 涂鸦红外网关
- 自制 ESP8266 + 红外发射头方案

---

## 3. HarmonyOS Connect 生态接入

### 3.1 鸿蒙智联架构

```
┌─────────────────────────────────────────────────────────────┐
│                  HarmonyOS Connect 生态                     │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   用户端                                                      │
│   ┌──────────────────┐  ┌──────────────────┐              │
│   │   智慧生活 App    │  │   数字管家 App    │              │
│   └──────────────────┘  └──────────────────┘              │
│            ↑                       ↑                       │
│            │                       │                       │
│            ▼                       ▼                       │
│   ┌─────────────────────────────────────────────┐          │
│   │              华为 IoT 云平台                    │          │
│   │   ┌──────────┐  ┌──────────┐  ┌──────────┐ │          │
│   │   │ 设备接入 │  │ 规则引擎 │  │ 数据存储 │ │          │
│   │   │   IoTDA  │  │          │  │          │ │          │
│   │   └──────────┘  └──────────┘  └──────────┘ │          │
│   └─────────────────────────────────────────────┘          │
│                            ↑                                 │
│                     MQTT/CoAP                               │
│                            ↑                                 │
│   ┌─────────────────────────────────────────────┐          │
│   │            智能设备端                        │          │
│   │  ┌────────┐ ┌────────┐ ┌────────┐          │          │
│   │  │ 空调   │ │ 灯光   │ │ 门锁   │ ...    │          │
│   │  └────────┘ └────────┘ └────────┘          │          │
│   └─────────────────────────────────────────────┘          │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

### 3.2 设备接入流程

```
步骤 1: 成为合作伙伴
   ↓
步骤 2: 创建产品 → 定义物模型（属性/命令/事件）
   ↓
步骤 3: 设备开发 → 集成 HiLink SDK / 鸿蒙系统
   ↓
步骤 4: 配网调试 → NFC 碰一碰 / 扫码配网
   ↓
步骤 5: 产品认证 → 华为认证测试
   ↓
步骤 6: 上架销售 → 进入智慧生活 App
```

### 3.3 物模型定义示例（空调）

```json
{
  "service_id": "air_conditioner",
  "properties": [
    {
      "property_name": "power_state",
      "data_type": "boolean",
      "description": "开关状态"
    },
    {
      "property_name": "temperature",
      "data_type": "integer",
      "range": [16, 30],
      "description": "设定温度"
    },
    {
      "property_name": "mode",
      "data_type": "string",
      "enum": ["cool", "heat", "fan", "dry", "auto"],
      "description": "运行模式"
    },
    {
      "property_name": "fan_speed",
      "data_type": "string",
      "enum": ["auto", "low", "medium", "high"],
      "description": "风速"
    }
  ],
  "commands": [
    {
      "command_name": "power_on",
      "description": "开机"
    },
    {
      "command_name": "power_off", 
      "description": "关机"
    },
    {
      "command_name": "set_temperature",
      "paras": {"temp": "int"},
      "description": "设置温度"
    }
  ]
}
```

---

## 4. 云平台接入方案

### 4.1 华为云 IoT 平台接入

**优势**：
- 与 HarmonyOS 原生集成
- 免费额度充足（100 万消息/月）
- 支持 MQTT/CoAP/LWM2M

**接入步骤**：

```
1. 注册华为云账号 → 开通 IoTDA 服务
   ↓
2. 创建产品 → 定义物模型
   ↓
3. 注册设备 → 获取 DeviceId/Secret
   ↓
4. 生成 MQTT 三元组
   (使用工具: https://iot-tool.obs-website.cn-north-4.myhuaweicloud.com/)
   ↓
5. 设备端集成 MQTT SDK → 连接华为云
   ↓
6. 应用端调用 API → 控制设备
```

**MQTT 连接参数**：

| 参数 | 说明 | 示例值 |
|------|------|--------|
| Broker | MQTT 服务器地址 | `a161fa3144.iot-mqtts.cn-north-4.myhuaweicloud.com` |
| Port | 端口号 | `1883` |
| ClientId | 客户端 ID | 设备唯一标识 |
| Username | 用户名 | `deviceId` |
| Password | 密码 | 密钥 |

### 4.2 阿里云 IoT 平台接入

**优势**：
- 生态丰富，支持众多品牌设备
- Link Kit SDK 完善
- 设备影子功能强大

**接入步骤**：

```
1. 注册阿里云账号 → 开通 IoT 物联网平台
   ↓
2. 创建产品 → 定义物模型
   ↓
3. 创建设备 → 获取 ProductKey/DeviceName/DeviceSecret
   ↓
4. 设备端集成 Link Kit SDK
   ↓
5. 应用端调用 OpenAPI
```

### 4.3 私有化部署方案（EMQX + 自建服务）

```
┌─────────────────────────────────────────────────────────────┐
│                    私有化部署架构                           │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   移动 App  ──HTTPS──→  业务服务器                         │
│                            │                               │
│                            │                               │
│                            ▼                               │
│                    ┌──────────────┐                        │
│                    │   EMQX      │                        │
│                    │  MQTT Broker │                        │
│                    └──────────────┘                        │
│                      ↑        ↑                            │
│               ┌──────┘        └──────┐                    │
│               ▼                       ▼                      │
│        ┌────────────┐         ┌────────────┐               │
│        │  设备 1   │         │  设备 2   │               │
│        │  (空调)  │         │  (灯光)  │               │
│        └────────────┘         └────────────┘               │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

**EMQX 部署**：

```bash
# Docker 快速部署
docker run -d --name emqx \
  -p 1883:1883 \
  -p 8083:8083 \
  -p 8883:8883 \
  -p 18083:18083 \
  emqx/emqx:5.0.10

# 访问控制台
# http://localhost:18083
# 默认账号: admin / public
```

---

## 5. 虚拟仿真测试方案

### 5.1 软件仿真方案

#### 5.1.1 MQTT 模拟服务器 + 虚拟设备

```
┌─────────────────────────────────────────────────────────────┐
│                    软件仿真架构                               │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   开发者电脑（localhost）                                    │
│   ┌─────────────────────────────────────────────────────┐  │
│   │                                                      │  │
│   │  ┌──────────────┐      ┌──────────────────────┐   │  │
│   │  │   EMQX       │      │   虚拟设备模拟器      │   │  │
│   │  │  MQTT Broker │◄────►│  - 空调模拟          │   │  │
│   │  │  :1883       │      │  - 灯光模拟          │   │  │
│   │  └──────────────┘      │  - 窗帘模拟          │   │  │
│   │         ▲              │  - 门锁模拟          │   │  │
│   │         │              └──────────────────────┘   │  │
│   │         │                                           │  │
│   │         ▼                                           │  │
│   │  ┌─────────────────────────────────────────────┐   │  │
│   │  │           SmartHomeAIHub App                 │   │  │
│   │  │   AI助手 ──► 设备控制 ──► 状态显示          │   │  │
│   │  └─────────────────────────────────────────────┘   │  │
│   │                                                      │  │
│   └─────────────────────────────────────────────────────┘  │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 5.1.2 虚拟设备模拟器实现

```typescript
// VirtualDeviceSimulator.ets
// 虚拟设备模拟器 - 用于开发测试阶段

export class VirtualDeviceSimulator {
  private deviceStates: Map<string, DeviceState> = new Map()
  private mqttClient: mqtt.MqttClient | null = null
  
  // 初始化虚拟设备
  initDevices(): void {
    // 虚拟空调
    this.deviceStates.set('ac_001', {
      id: 'ac_001',
      type: 'air_conditioner',
      name: '客厅空调',
      power: false,
      temperature: 26,
      mode: 'cool',
      fanSpeed: 'medium'
    })
    
    // 虚拟灯光
    this.deviceStates.set('light_001', {
      id: 'light_001',
      type: 'light',
      name: '客厅灯',
      on: true,
      brightness: 80,
      color: '#FFFFFF'
    })
    
    // 虚拟窗帘
    this.deviceStates.set('curtain_001', {
      id: 'curtain_001',
      type: 'curtain',
      name: '客厅窗帘',
      open: false,
      position: 0  // 0-100%
    })
    
    // 虚拟门锁
    this.deviceStates.set('lock_001', {
      id: 'lock_001',
      type: 'smart_lock',
      name: '智能门锁',
      locked: true,
      battery: 85
    })
  }
  
  // 处理控制指令
  handleCommand(deviceId: string, command: ControlCommand): boolean {
    const device = this.deviceStates.get(deviceId)
    if (!device) return false
    
    switch (command.type) {
      case 'power':
        device.power = command.value
        break
      case 'temperature':
        device.temperature = command.value
        break
      case 'light_brightness':
        device.brightness = command.value
        break
      // ... 其他指令
    }
    
    // 模拟设备响应延迟
    setTimeout(() => {
      this.reportStatus(deviceId)
    }, 200)
    
    return true
  }
  
  // 报告设备状态（模拟设备上报）
  reportStatus(deviceId: string): void {
    const device = this.deviceStates.get(deviceId)
    if (device && this.mqttClient) {
      const topic = `smart_home/device/${deviceId}/status`
      this.mqttClient.publish(topic, JSON.stringify(device))
    }
  }
}
```

#### 5.1.3 MQTTX 桌面客户端测试

**MQTTX** 是一款跨平台 MQTT 桌面客户端，可用于：
- 连接 MQTT 服务器
- 订阅/发布主题
- 模拟设备消息
- 测试消息流转

**下载地址**：https://mqttx.app/

**测试配置**：

| 配置项 | 值 |
|-------|-----|
| Broker | `ws://localhost:1883` 或 `mqtt://localhost:1883` |
| Client ID | `test_device_001` |
| Topic (订阅) | `smart_home/device/+/status` |
| Topic (发布) | `smart_home/device/ac_001/command` |

**测试消息格式**：

```json
// 发送控制命令
{
  "device_id": "ac_001",
  "command": "set_temperature",
  "params": {
    "temperature": 24
  },
  "timestamp": 1709900000000
}

// 设备状态上报
{
  "device_id": "ac_001",
  "power": true,
  "temperature": 24,
  "mode": "cool",
  "timestamp": 1709900000200
}
```

### 5.2 硬件模拟器方案

#### 5.2.1 开发板模拟真实设备

```
┌─────────────────────────────────────────────────────────────┐
│                    开发板模拟方案                            │
├─────────────────────────────────────────────────────────────┤
│                                                             │
│   硬件层                                                      │
│   ┌─────────────────────────────────────────────────────┐   │
│   │                                                      │   │
│   │   ┌────────────┐   模拟     ┌──────────────────┐   │   │
│   │   │ Hi3861    │ ────────► │ 红外发射 + LED   │   │   │
│   │   │ 开发板    │           │ + 显示屏        │   │   │
│   │   └────────────┘           └──────────────────┘   │   │
│   │         │                                                │   │
│   │         │  MQTT 协议                                      │   │
│   │         ▼                                                │   │
│   │   ┌────────────┐                                         │   │
│   │   │ EMQX 本地 │  ◄──────  真实 App 控制                  │   │
│   │   │ MQTT 服务器│                                         │   │
│   │   └────────────┘                                         │   │
│   │                                                      │   │
│   └─────────────────────────────────────────────────────┘   │
│                                                             │
└─────────────────────────────────────────────────────────────┘
```

#### 5.2.2 推荐开发板配置

| 开发板 | 用途 | 特点 | 价格 |
|--------|------|------|------|
| **Hi3861** | WiFi 设备模拟 | 鸿蒙官方，支持 WiFi + MQTT | ¥30-50 |
| **ESP8266** | 通用设备模拟 | Arduino 生态，红外支持好 | ¥15-25 |
| **ESP32** | 高级设备模拟 | 双核 + 蓝牙 + WiFi | ¥30-50 |
| **STM32 + ESP8266** | 专业方案 | 性能强 + 联网能力 | ¥50-80 |

#### 5.2.3 红外空调模拟器实现

```
硬件连接：
ESP8266 D5 (GPIO14) ─── 红外发射管 ───→ 空调（接收）
```

```cpp
// ESP8266 红外空调模拟器代码框架
#include <ESP8266WiFi.h>
#include <PubSubClient.h>
#include <IRsend.h>

// WiFi 配置
const char* ssid = "Your_WiFi";
const char* password = "Your_Password";

// MQTT 服务器
const char* mqtt_server = "192.168.1.100";
const int mqtt_port = 1883;

// 红外发射引脚
IRsend irsend(14);  // GPIO14 = D5

// 空调状态
struct AirConditioner {
  bool power;
  int temperature;
  String mode;  // cool, heat, fan, dry, auto
  String fanSpeed;  // auto, low, medium, high
} ac;

void callback(char* topic, byte* payload, unsigned int length) {
  // 解析 MQTT 消息
  StaticJsonDocument<256> doc;
  deserializeJson(doc, payload);
  
  String command = doc["command"];
  
  if (command == "power") {
    ac.power = doc["value"];
  } 
  else if (command == "temperature") {
    ac.temperature = doc["value"];
  }
  else if (command == "control") {
    // 发送红外命令到空调
    sendACCommand();
  }
  
  // 上报状态
  reportStatus();
}

void sendACCommand() {
  // 发送空调红外控制码
  // 使用 IRremoteESP8266 库
  if (ac.power) {
    // 发送开机码
    irsend.sendRaw(acPowerOn, 67, 38);
  } else {
    // 发送关机码
    irsend.sendRaw(acPowerOff, 67, 38);
  }
}

void reportStatus() {
  // 状态上报到 MQTT
  char payload[256];
  sprintf(payload, 
    "{\"power\":%s,\"temp\":%d,\"mode\":\"%s\"}",
    ac.power ? "true" : "false",
    ac.temperature,
    ac.mode.c_str()
  );
  client.publish("device/ac/status", payload);
}
```

### 5.3 第三方模拟平台

#### 5.3.1 涂鸦智能模拟

| 方案 | 说明 | 成本 |
|------|------|------|
| 涂鸦 IoT 平台 | 免费创建虚拟设备，可模拟设备控制 | 免费 |
| 涂鸦云 API | 调用接口模拟设备状态 | 免费额度 |
| 涂鸦小程序 | 使用官方 App 控制虚拟设备 | 免费 |

#### 5.3.2 Home Assistant 模拟

```
Home Assistant
     │
     ├── 集成 MQTT Broker
     │        └── 接收/发送设备消息
     │
     ├── 虚拟设备插件
     │        ├── 虚拟空调
     │        ├── 虚拟灯光
     │        └── 虚拟传感器
     │
     └── REST API
              └── App 调用
```

**配置虚拟设备**：

```yaml
# configuration.yaml
mqtt:
  broker: localhost
  port: 1883

switch:
  - name: "虚拟空调电源"
    state_topic: "home/ac/power"
    command_topic: "home/ac/set_power"
    payload_on: "ON"
    payload_off: "OFF"

climate:
  - name: "虚拟空调"
    current_temperature_topic: "home/ac/temp_current"
    temperature_state_topic: "home/ac/temp_target"
    temperature_command_topic: "home/ac/set_temp"
    modes: ["off", "cool", "heat", "fan_only"]
```

---

## 6. 项目改造实施路径

### 6.1 阶段一：本地虚拟设备仿真（1-2周）

**目标**：不依赖真实设备，实现完整的控制链路

```
AI助手 ←→ DeviceStore ←→ MQTT Client ←→ EMQX ←→ 虚拟设备
                              ↑
                        本地模拟服务
```

**实施内容**：

1. **集成 MQTT 客户端库**
   ```bash
   ohpm install @ohos/mqtt
   ```

2. **创建设备控制服务**
   ```typescript
   // DeviceControlService.ets
   import { mqtt } from '@ohos/mqtt'
   
   export class DeviceControlService {
     private mqttClient: mqtt.MqttClient | null = null
     
     // 连接本地 MQTT 服务器
     async connect(brokerUrl: string): Promise<boolean> {
       // 实现连接逻辑
     }
     
     // 发送控制指令
     async sendCommand(deviceId: string, command: ControlCommand): Promise<boolean> {
       // 发布命令到 MQTT 主题
     }
     
     // 订阅设备状态
     subscribeStatus(deviceId: string, callback: StatusCallback): void {
       // 订阅设备状态主题
     }
   }
   ```

3. **创建本地模拟设备服务**
   ```typescript
   // LocalDeviceSimulator.ets
   export class LocalDeviceSimulator {
     // 模拟各类设备的响应
     handleACCommand(cmd: ACCommand): void { /* ... */ }
     handleLightCommand(cmd: LightCommand): void { /* ... */ }
   }
   ```

4. **修改 AI 助手响应处理**
   ```typescript
   // AIService.ets - 添加设备控制能力
   
   // 解析用户意图并执行控制
   async processSmartHomeCommand(userMessage: string): Promise<ControlResult> {
     // 1. 使用 LLM 解析用户意图
     const intent = await this.parseIntent(userMessage)
     
     // 2. 构建控制指令
     const command = this.buildCommand(intent)
     
     // 3. 发送到设备
     const result = await deviceService.sendCommand(command)
     
     // 4. 返回执行结果
     return result
   }
   ```

### 6.2 阶段二：云平台接入（2-3周）

**目标**：接入真实云平台，支持远程控制

```
App ←→ 华为云 IoT ←→ 真实设备
  ↑
AI助手
```

**实施内容**：

1. 注册华为云账号，开通 IoTDA
2. 创建产品，定义物模型
3. 设备端集成 MQTT SDK
4. 应用端对接云端 API

### 6.3 阶段三：真实设备接入（3-4周）

**目标**：控制真实智能家居设备

**红外控制方案**：

```
手机 App ← WiFi → ESP8266/Hi3861 ← IR → 空调
```

**Matter/鸿蒙智联方案**：

```
手机 App ←→ 华为云 ←→ 鸿蒙认证设备
```

---

## 7. 推荐开发套件

### 7.1 入门级（预算 ¥100-300）

| 组件 | 型号 | 数量 | 价格 | 用途 |
|------|------|------|------|------|
| 主控板 | ESP8266 NodeMCU | 2 | ¥30 | WiFi + 红外控制 |
| 红外发射 | 940nm 红外 LED | 4 | ¥5 | 红外信号发射 |
| 红外接收 | VS1838B | 1 | ¥3 | 学习遥控信号 |
| 温湿度传感器 | DHT11 | 1 | ¥5 | 环境数据采集 |
| LED 模块 | RGB LED | 2 | ¥4 | 状态指示 |
| 杜邦线 | 40P | 1 | ¥3 | 连接线 |
| **合计** | | | **¥50** | |

### 7.2 进阶级（预算 ¥300-800）

| 组件 | 型号 | 数量 | 价格 | 用途 |
|------|------|------|------|------|
| 主控板 | Hi3861 开发板 | 1 | ¥80 | 鸿蒙官方开发板 |
| 主控板 | ESP32 DevKit | 2 | ¥70 | 高性能双核 |
| 显示屏 | 0.96 OLED I2C | 2 | ¥25 | 状态显示 |
| 传感器套装 | DHT11 + MQ2 + 光敏 | 1 | ¥20 | 环境监测 |
| 继电器模块 | 4路 5V | 1 | ¥12 | 开关控制 |
| 红外套装 | 发射+接收+遥控 | 1 | ¥15 | 红外控制 |
| 电机驱动 | L298N | 1 | ¥8 | 窗帘电机 |
| **合计** | | | **¥230** | |

### 7.3 专业级（预算 ¥1000+）

| 组件 | 型号 | 价格 | 用途 |
|------|------|------|------|
| Hi3861 鸿蒙套件 | 润和满天星 | ¥300 | 官方认证开发 |
| Hi3861 传感器扩展板 | E53 系列 | ¥150 | 温湿度，光照等 |
| 鸿蒙开发板套装 | 小熊派 BearPi | ¥400 | 完整学习套件 |
| 云服务 | 华为云 IoTDA | 免费 | 设备接入 |

---

## 附录

### A. MQTT 主题设计规范

```
smart_home/
├── device/
│   ├── {device_id}/
│   │   ├── command      # App → 设备
│   │   ├── status       # 设备 → App
│   │   └── config       # 配置下发
├── app/
│   ├── command          # App 控制命令
│   └── response         # 设备响应
└── system/
    ├── online           # 设备上线
    └── offline          # 设备离线
```

### B. 常用 MQTT 客户端库

| 平台 | 库名称 | 安装命令 | 特点 |
|------|--------|---------|------|
| HarmonyOS | `@ohos/mqtt` | `ohpm install @ohos/mqtt` | 官方库 |
| Android | Eclipse Paho | `implementation 'org.eclipse.paho:org.eclipse.paho.client.mqttv3:1.2.5'` | 成熟稳定 |
| iOS | CocoaMQTT | `pod 'CocoaMQTT'` | Swift 友好 |
| Web | MQTT.js | `npm install mqtt` | 浏览器支持 |

### C. 参考资料

- [华为云 IoT 文档](https://console.huaweicloud.com/iotda)
- [HarmonyOS Connect 开发指南](https://developer.huawei.com/consumer/cn/doc/harmonyos-guides-V2/connect-overview-0000001066863512-V2)
- [OpenHarmony MQTT 开发示例](https://gitee.com/openharmony/docs/tree/master/zh-cn/device-dev)
- [EMQX 官方文档](https://www.emqx.com/docs)
- [MQTTX 客户端工具](https://mqttx.app)

---

**文档版本**：v1.0  
**更新时间**：2026-06-08  
**适用项目**：SmartHomeAIHub
